.DS_Store
**/.DS_Store

# Env files: ignore every variant, including dotted, per-environment and
# backup forms. This repo had no env rule at all, so `.env` and every
# dotted variant were unignored and committable.
*.env
.env
.env.*
!.env.example

# Standalone review mocks. Working material, never served, and deliberately
# untracked: CLAUDE.md has said so since before they were briefly committed.
# The approved copies live outside this repo at ~/biowellth-mocks-approved/.
mocks/
